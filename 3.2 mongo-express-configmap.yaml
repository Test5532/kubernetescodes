apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config-db-name-configmap # This is name of config map and it matches environment variable configMapKeyRef/name. 
  namespace: mongo
data:
  me_config_mongodb-port : 27017
  me_config_mongodb-server : mongo-service  # mongodb service name